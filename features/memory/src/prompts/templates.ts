export const IMAGE_PROMPT_TEMPLATE = `
你是一名图像提示词工程专家。根据用户的一段记忆内容，生成用于图像生成（英文）的稳定提示词，尽可能细节化与具象化，包含景别、光线、色彩、情绪与年代气息。

要求：
- 输出仅为英文短句，避免换行与引号
- 融合以下参数：
  - 内容：{content}
  - 地点：{location}
  - 年代：{year}
  - 氛围/情绪：{emotions}
  - 用户默认风格：{style}
  - 宽高比：{aspect}

返回：一行英文提示词。`;

export const KEYWORDS_PROMPT_TEMPLATE = `
你是一名时代文化与社会研究专家，请基于给定的年代范围与地区，生成一组能唤醒记忆的关键词（中文），覆盖社会事件、流行文化、科技、教育、城市地标、食物、音乐/影视、流行语等类别。

参数：
- 年代范围：{startYear}–{endYear}
- 地区：{region}
- 语言：{language}

要求：
- 输出JSON：{"keywords": ["关键词1", "关键词2", ...]}，数量不少于50个，按时间递进大致均匀分布；避免重复与过于笼统的词。
`;

export const RADIO_YEAR_SCRIPT_TEMPLATE = `
你是一名资深广播电台撰稿人。请以中文为主（如 language 为 en-US 则使用英文），为指定年份撰写一篇电台播报脚本，长度不少于3000字，内容覆盖该年在 {region} 相关的社会、经济、文化、科技、教育、生活方式、城市风貌、代表人物与事件，结合时代细节，并加入与日常生活相关的场景描写，语气亲切、真挚、具画面感。

参数：
- 年份：{year}
- 地区：{region}
- 用户出生年份：{birthYear}
- 语言：{language}

返回：直接输出完整脚本文本（不含任何 JSON 或解释）。`;
